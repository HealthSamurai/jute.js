{
  "name": "jute",
  "version": "0.0.1",
  "dependencies": {
  },
  "devDependencies": {
    "coffee-script": "latest",
    "coffee": "latest",
    "mocha": "latest",
    "js-yaml": "latest",
    "pegjs": "latest"
  },
  "engines": {
    "node": ">=0.12.0"
  },
  "scripts": {
    "build": "`npm bin`/coffee -c -o lib/ src/*.coffee",
    "watch": "`npm bin`/coffee -c -w -o lib/ src/*.coffee",
    "test": "`npm bin`/mocha --compilers coffee:coffee-script/register spec/runner.coffee || echo 'Tests failed'",
    "peg": "`npm bin`/pegjs src/grammar.pegjs lib/parser.js"
  }
}
